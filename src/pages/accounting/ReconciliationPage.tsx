import { PageShell } from '@/components/shared/PageShell'
import { RefreshCcw } from 'lucide-react'

export function ReconciliationPage() {
  return <PageShell titleKey="nav.reconciliation" icon={RefreshCcw} />
}
